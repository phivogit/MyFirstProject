int main() {
    int choice;
    double temp;
    cin >> choice >> temp;
    if (choice == 1) cout << (9.0/5)*temp + 32;
    else if (choice = 2) cout << (5.0/9) * (temp - 32);
    else cout << "Invalid option";
}